[phases.setup]
commands = [
  ""
]
aptPkgs = ["wget", "curl", "python", "python-pip", "libpq-dev", "python-dev"]

[phases.build]
commands = [
  "pip install --upgrade build setuptools",
  "pip install -r requirements.txt"
]

[start]
cmd = 'python app.py'